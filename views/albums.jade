extends layout

block content
	a(href='/')
	ul
		each item in items
			.album-list
				li
					.artist= item.artist 
					.album= item.album 
					.release-year
						if item['release-year']
							= 'Release Year: ' + item['release-year']
						else
							= 'Release Year: unknown'
					.rating
						if item.rating
							- for (var i = 0; i < item.rating; i++)
								= '*'
						else
							= 'no rating'
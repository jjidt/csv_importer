extends layout

block content
	a(href='/')
	ul
		each item in items
			.album-list
				li
					.artist= item.artist 
					.album= item.album 
					.release-year= 'Release Year: ' + item['release-year']
					.rating= 'Rating: ' + item.rating